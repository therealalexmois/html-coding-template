include ../../../node_modules/bemto.pug/bemto.pug
include ../page/page

-
  set_bemto_settings({
    prefix: '',
    element: '__',
    modifier: '--'
  })

doctype html
html
        include ../head/head
        +page(class=pageMod ? '_' + pageMod : '')

        +scripts(['app.min.js'])